# peek-test-app

Tiny Vite app used as a dogfood target for peek.dev JIT previews.

Trigger a preview from any branch or SHA.

Mount verification rev 2 - primary proxies /api to the mounted peek-test-api service on its offset port.
